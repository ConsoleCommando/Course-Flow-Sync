<!DOCTYPE html>
<html>
<head>
  
<title>CourseFlowSync</title>
  
<style>

body { background-color: blue; background-image: url(""); width: 35em; margin: 0 auto;
font-family: Tahoma, Verdana, Arial, sans-serif; }
h1{ color: gold; text-align: center;}

form {
    text-align: center;
}
  
label{color: gold;}

.center {
  padding: 200px 0;
  text-align: center;
}
</style>
</head>

<body>
<div class="center"> 
  
<h1>CourseFlowSync</h1>
  <form id="myUrl" action="">

  <label for="urlLink">Please enter a URL:</label>
  <input type="text" id="urlInput" name="urlInput"><br><br>
  
  <input type="submit" value="Submit">
  <input type="reset" value="Clear">
</form>

  <script>
     const puppeteer = require('puppeteer')

async function getVisual() {
	try {
		const URL = 'https://chattanooga.gov'
		const browser = await puppeteer.launch()

		const page = await browser.newPage()
		await page.goto(URL)

		await page.screenshot({ path: 'screenshot.png' })
		await page.pdf({ path: 'page.pdf' })

		await browser.close()
	} catch (error) {
		console.error(error)
	}
}

getVisual()
  </script>


</body>
</html>
